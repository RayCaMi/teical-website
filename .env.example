# Copie para .env para desenvolvimento local (opcional — o padrão já é o backend local)
# Em produção, configure VITE_API_URL nas variáveis de build do Cloudflare Pages
VITE_API_URL=http://127.0.0.1:8000
